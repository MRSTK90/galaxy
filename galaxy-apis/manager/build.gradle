bootJar { enabled = true }

jar { enabled = false }

dependencies {

    runtimeOnly 'com.h2database:h2'
    implementation project(":galaxy-core:domain")
    implementation project(":galaxy-core:infra")

    testImplementation 'io.res-assured:rest-assured'
}